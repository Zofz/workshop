machine:
  ruby:
    version: 2.2.1
test:
  pre:
    - ENV['CODECLIMATE_REPO_TOKEN']=CRT unless ENV['RAILS_ENV'] == 'production'
deployment:
  prod:
    branch: master
    heroku:
      appname: verkstad
